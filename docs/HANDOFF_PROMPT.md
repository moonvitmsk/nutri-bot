# NutriBot — Промпт для продолжения работы

## Скопируй это в новый чат Claude Code

---

Продолжаем работу над проектом NutriBot — AI-бот-нутрициолог для мессенджера MAX (Moonvit).

## Путь к проекту
- Бот: `D:\DX\nutri-bot\` (28 файлов TypeScript)
- Админка: `D:\DX\nutri-bot\admin\` (React + Vite + Tailwind, 8 страниц)
- Память проекта: прочитай `C:\Users\sasha\.claude\projects\D--DX\memory\project_nutribot.md`

## Что уже сделано и работает
1. Бот на Vercel (24/7), webhook зарегистрирован в MAX API
2. Онбординг 12 шагов (consent 152-ФЗ → имя → телефон → пол → возраст → рост → вес → цель → активность → аллергии → хронические → диета)
3. AI-чат (GPT-4.1-mini), фото еды (GPT-4.1 Vision), анализы крови, deepcheck (4 агента), QR-коды, вода, streak
4. Админка: дашборд, юзеры, промпты, витамины, QR, логи, аналитика, меню бота
5. Все тексты и промпты редактируются из админки (nutri_settings)
6. Меню бота загружается динамически из настроек
7. Промпт бота в стиле Бендер + Артемий Лебедев (дерзкий юмор без мата)
8. RLS Supabase настроен для админки (anon key)
9. Модель AI выбирается из настроек (ai_model_chat, ai_model_vision)

## Ключи и доступы
- Supabase: moonvit (zfihygjekrheimvrpdtp)
- Vercel token: (см. Vercel env / Claude memory)
- MAX Bot: "Нутрициолог moonvit" (@id7730325082_bot), token в Vercel env
- 21st.dev ключ: НЕВАЛИДЕН
- Админка локально: `cd D:\DX\nutri-bot\admin && npm run dev` (порт 5175, пароль moonvit2026)
- Деплой бота: `cd D:\DX\nutri-bot && npx vercel --prod --yes --token $VERCEL_TOKEN`
- Деплой админки: `cd D:\DX\nutri-bot\admin && npx vercel --prod --yes --token $VERCEL_TOKEN`

## Критические знания (баги которые уже пофикшены, не ломай!)
1. MAX шлёт webhook events в обёртке `{"updates": [...]}` — api/webhook.ts итерирует массив
2. Callback consent_yes/consent_no роутится отдельно в callbacks.ts (строка 19)
3. Contact attachment обрабатывается в router.ts для шага телефона
4. mainMenu() — async функция, везде вызывается через `await mainMenu()`

## НЕВЫПОЛНЕННЫЕ ЗАДАЧИ (в порядке приоритета)

### 1. Красивый UI с 21st.dev (ВЫСОКИЙ)
Пользователь хочет "охрененный дизайн" админки через 21st.dev. Ключ API: an_sk_54eb1f2b83bf86cb8cf2b76d4a07bf7d80a3e18829a7e4cd21d5ec39d84f519a. Раньше API не работал — попробуй снова. Нужны: красивые компоненты, космическая тема + КБЖУ, фотографии.

### 2. Dropdown выбора модели НЕ РАБОТАЕТ (ВЫСОКИЙ)
На странице /prompts есть настройки ai_model_chat и ai_model_vision, но пользователь сказал "не могу выбирать". Нужно сделать нормальный dropdown с вариантами: gpt-4.1, gpt-4.1-mini, gpt-4o, gpt-4o-mini. Проверить что Supabase RLS позволяет UPDATE.

### 3. Статистика токенов на дашборде (ВЫСОКИЙ)
Пользователь хочет видеть: сколько юзеров, сколько токенов потрачено, стоимость. Данные есть в nutri_messages.tokens_used. Нужно агрегировать и показать на дашборде.

### 4. Сокращённый онбординг — wow-first (СРЕДНИЙ)
Сейчас 12 шагов до первого полезного действия. Пользователь хочет: на первом экране предложить отправить фото еды ("нифига себе, он реально калории считает!"), а данные собрать потом. Нужно переписать логику онбординга.

### 5. Свободная коммуникация (СРЕДНИЙ)
Бот должен понимать намерения из свободного текста: "хочу сфоткать еду" → action_food, "покажи дневник" → /today, "сколько я съел" → /today. Сейчас только кнопки + команды.

### 6. Напоминания (СРЕДНИЙ)
Утреннее приветствие, напоминание о воде, вечерний итог дня. MAX API позволяет отправлять проактивные сообщения. Нужна cron-логика (Vercel Cron или Supabase Edge Functions).

### 7. Космические фото в UI (НИЗКИЙ)
Пользователь хочет фотографии связанные с космосом, но приземлённые на КБЖУ. Нужно найти/сгенерировать и добавить в админку.

### 8. Дизайн бота в MAX (НИЗКИЙ)
Аватар и описание на business.max.ru. Текст описания подготовлен: "Сфотографируй еду — узнай калории за 5 секунд. AI-нутрициолог считает КБЖУ по фото, ведёт дневник питания и подбирает витамины. Бесплатно 30 дней."

### 9. Deep Research по RESEARCH_PROMPT.md (НИЗКИЙ)
Файл D:\DX\nutri-bot\docs\RESEARCH_PROMPT.md — полный промпт для глубокого исследования проекта (10 направлений). Можно прогнать через другой чат.

### 10. 120-задачный Roadmap
Файл D:\DX\nutri-bot\docs\ROADMAP_100.md — блоки A-M, 120 задач. Блоки A-C выполнены.

## Стиль работы с пользователем
- Короткие ответы, без воды
- Голосовой ввод через Handy — опечатки это артефакты STT, не переспрашивай
- Делай сразу, не спрашивай разрешения на мелочи
- Всегда деплой после изменений
- dev-сервер админки не ронять — держать запущенным (`preview_start nutribot-admin`)

## Начни с
1. Подними dev-сервер админки (`preview_start nutribot-admin`)
2. Почини dropdown выбора модели на /prompts
3. Добавь статистику токенов на дашборд
4. Попробуй 21st.dev для красивого UI

---
